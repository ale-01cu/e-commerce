from django.apps import AppConfig

class UserHistoryConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.user_history'
    verbose_name = 'Historial de Usuario'
