from django.apps import AppConfig

class RecommendationSystemConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.recommendation_system'
    verbose_name = 'Sistema de Recomendaciones'
